import { extendTheme } from "@chakra-ui/react";

const theme = extendTheme({
  styles: {
    global: {
      body: {
        backgroundColor: "#cbe8da",
        textAlign: "center",
        color: "#00733b",
      },
    },
  },
});
export default theme;
